{
  "routes": [
      {
        "regexp": {
          "exp": "([a-z0-9-].*).(?P<cluster>[a-z0-9-].*).internal.example.com",
          "cluster_group_name": "cluster",
          "kedge_url": "{{.Cluster}}.kedge.ext.example.com"
        }
      }
    ]
}